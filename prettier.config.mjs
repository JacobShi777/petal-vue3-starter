export default {
  // 行末分号, 默认true
  semi: false,
  // 缩进长度
  tabWidth: 2,
  // 使用单引号
  singleQuote: true,
  //单行长度
  printWidth: 120,
  // 尽可能使用尾随逗号（包括函数参数）,默认 none,可选 none|es5|all
  // es5 包括es5中的数组、对象
  // all 包括函数对象等所有可选
  trailingComma: 'all',
  // 在对象前后添加空格-eg: { foo: bar } 默认true
  bracketSpacing: true,
  // 箭头函数参数括号 默认avoid 可选 avoid| always
  // avoid 能省略括号的时候就省略 例如x => x
  // always 总是有括号
  arrowParens: 'always',
  // 行尾换行格式
  // endOfLine: 'auto',
  // html空格敏感度
  // htmlWhitespaceSensitivity: 'ignore',
  // 不对vue中的script及style标签缩进
  // vueIndentScriptAndStyle: false,
}
